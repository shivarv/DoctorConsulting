# Runtime dependencies — everything the deployed API needs, and nothing else.
# The Dockerfile installs only this file, so anything added here ships to
# production. Test and tooling deps go in requirements-dev.txt.

fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9
python-dotenv>=1.0

# postgres
psycopg[binary]>=3.2
psycopg-pool>=3.2
